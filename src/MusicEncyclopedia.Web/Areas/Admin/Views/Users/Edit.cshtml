@model UserEditViewModel
@{
    ViewData["Title"] = $"Edit User: {Model.Email}";
    ViewData["ActiveMenu"] = "Users";
}

<form method="post" action="/admin/users/@Model.Id/Edit">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id" />
    <div asp-validation-summary="ModelOnly" class="mb-4 px-4 py-3 rounded-md bg-error-soft border border-error-border text-error-hover text-sm" role="alert"></div>
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <div class="lg:col-span-2 space-y-6">
            <div class="bg-surface rounded-lg shadow-sm border border-rule p-5">
                <h2 class="text-lg font-semibold text-ink mb-1">@Model.Email</h2>
                <p class="text-sm text-ink-muted mb-4">نقش‌ها و دسترسی‌های مستقیم را تخصیص دهید. دسترسی‌های نقش در ورود بعدی به‌صورت خودکار اعمال می‌شوند.</p>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <span class="block text-sm font-medium text-ink-secondary mb-2">نقش‌ها</span>
                        <div class="space-y-2 border border-rule rounded-md p-3 max-h-56 overflow-y-auto">
                            @foreach (var role in Model.AllRoles)
                            {
                                <label class="flex items-center gap-2 text-sm cursor-pointer hover:bg-paper rounded px-1 py-0.5">
                                    <input type="checkbox" name="SelectedRoles" value="@role" @(Model.SelectedRoles.Contains(role) ? "checked" : "") class="rounded border-rule-strong text-accent focus-visible:ring-accent" />
                                    <span>@role</span>
                                </label>
                            }
                            @if (Model.AllRoles.Count == 0)
                            {
                                <p class="text-xs text-ink-muted">نقشی تعریف نشده است. <a href="/admin/roles" class="text-accent hover:underline">ایجاد نقش</a> ابتدا.</p>
                            }
                        </div>
                    </div>
                    <div>
                        <span class="block text-sm font-medium text-ink-secondary mb-2">وضعیت حساب</span>
                        <div class="space-y-3 border border-rule rounded-md p-3">
                            <label class="flex items-center gap-2 text-sm cursor-pointer">
                                <input type="checkbox" asp-for="IsLockedOut" class="rounded border-rule-strong text-error focus-visible:ring-error" />
                                <span>قفل کردن این حساب</span>
                            </label>
                            <p class="text-xs text-ink-muted">کاربران قفل‌شده نمی‌توانند تا زمانی که قفل برداشته نشده وارد شوند.</p>
                        </div>
                    </div>
                </div>
            </div>
            <div class="bg-surface rounded-lg shadow-sm border border-rule p-5">
                <h2 class="text-lg font-semibold text-ink mb-1">دسترسی‌ها</h2>
                <p class="text-sm text-ink-muted mb-4">ادعاهای دسترسی مستقیم. این‌ها با هر دسترسی اعطاشده از طریق نقش‌ها ترکیب می‌شوند.</p>
                <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-2 border border-rule rounded-md p-3 max-h-96 overflow-y-auto">
                    @foreach (var permission in Model.AllPermissions)
                    {
                        <label class="flex items-center gap-2 text-sm cursor-pointer hover:bg-paper rounded px-1 py-0.5">
                            <input type="checkbox" name="SelectedPermissions" value="@permission" @(Model.SelectedPermissions.Contains(permission) ? "checked" : "") class="rounded border-rule-strong text-accent focus-visible:ring-accent" />
                            <span class="font-mono text-xs">@permission</span>
                        </label>
                    }
                </div>
            </div>
        </div>
        <div class="space-y-6">
            <div class="bg-surface rounded-lg shadow-sm border border-rule p-5">
                <h2 class="text-lg font-semibold text-ink mb-4">عملیات</h2>
                <button type="submit" class="w-full px-4 py-2.5 bg-accent text-on-accent text-sm font-medium rounded-md hover:bg-accent-hover transition-colors flex items-center justify-center gap-2"><i aria-hidden="true" class="fas fa-save"></i> ذخیره تغییرات</button>
                <a href="/admin/users" class="w-full block text-center mt-3 px-4 py-2.5 text-sm text-ink-secondary bg-paper-muted rounded-md hover:bg-rule transition-colors"><i aria-hidden="true" class="fas fa-arrow-left me-1"></i> بازگشت به کاربران</a>
                <hr class="border-rule my-3" />
                <form method="post" action="/admin/users/@Model.Id/Delete" onsubmit="return confirm('Are you sure you want to delete this user?');">@Html.AntiForgeryToken()<button type="submit" class="w-full px-4 py-2 text-sm text-error border border-error-border rounded-md hover:bg-error-soft transition-colors flex items-center justify-center gap-2"><i aria-hidden="true" class="fas fa-trash-alt"></i> حذف کاربر</button></form>
            </div>
        </div>
    </div>
</form>
