@model CitationEditViewModel
@{
    ViewData["Title"] = "Edit Citation";
    ViewData["ActiveMenu"] = "Citations";
}

<form method="post" action="/admin/citations/@Model.CitationId/Edit">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="CitationId" />
    <div asp-validation-summary="ModelOnly" class="mb-4 px-4 py-3 rounded-md bg-red-50 border border-red-200 text-red-800 text-sm" role="alert"></div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <div class="lg:col-span-2 space-y-6">
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-5">
                <h2 class="text-lg font-semibold text-gray-900 mb-4">Source & Entity</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label asp-for="EntityTypeId" class="block text-sm font-medium text-gray-700 mb-1"></label>
                        <select asp-for="EntityTypeId" class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                            <option value="">-- Select Entity Type --</option>
                            @foreach (var et in Model.EntityTypes)
                            {
                                <option value="@et.EntityTypeId">@et.Name (@et.Code)</option>
                            }
                        </select>
                        <span asp-validation-for="EntityTypeId" class="text-xs text-red-600"></span>
                    </div>
                    <div>
                        <label asp-for="EntityId" class="block text-sm font-medium text-gray-700 mb-1"></label>
                        <input asp-for="EntityId" type="number" class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500" placeholder="Entity ID" />
                        <span asp-validation-for="EntityId" class="text-xs text-red-600"></span>
                    </div>
                    <div>
                        <label asp-for="SourceId" class="block text-sm font-medium text-gray-700 mb-1"></label>
                        <div class="flex gap-2">
                            <select asp-for="SourceId" class="flex-1 px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                                <option value="">-- Select Source --</option>
                                @foreach (var src in Model.Sources)
                                {
                                    <option value="@src.SourceId">@src.Title</option>
                                }
                            </select>
                            <button type="button" id="btn-create-source" class="px-3 py-2 bg-gray-100 text-gray-600 text-sm rounded-md hover:bg-gray-200 transition-colors" title="Create New Source">
                                <i class="fas fa-plus"></i>
                            </button>
                        </div>
                        <span asp-validation-for="SourceId" class="text-xs text-red-600"></span>
                    </div>
                    <div>
                        <label asp-for="FieldName" class="block text-sm font-medium text-gray-700 mb-1"></label>
                        <input asp-for="FieldName" class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500" />
                        <span asp-validation-for="FieldName" class="text-xs text-red-600"></span>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-5">
                <h2 class="text-lg font-semibold text-gray-900 mb-4">Citation Details</h2>
                <div class="space-y-4">
                    <div>
                        <label asp-for="Quote" class="block text-sm font-medium text-gray-700 mb-1"></label>
                        <textarea asp-for="Quote" rows="4" class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500"></textarea>
                        <span asp-validation-for="Quote" class="text-xs text-red-600"></span>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <div>
                            <label asp-for="PageNumber" class="block text-sm font-medium text-gray-700 mb-1"></label>
                            <input asp-for="PageNumber" class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500" />
                            <span asp-validation-for="PageNumber" class="text-xs text-red-600"></span>
                        </div>
                        <div>
                            <label asp-for="AccessedDate" class="block text-sm font-medium text-gray-700 mb-1"></label>
                            <input asp-for="AccessedDate" type="date" class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500" />
                            <span asp-validation-for="AccessedDate" class="text-xs text-red-600"></span>
                        </div>
                        <div>
                            <label asp-for="Url" class="block text-sm font-medium text-gray-700 mb-1"></label>
                            <input asp-for="Url" type="url" class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500" />
                            <span asp-validation-for="Url" class="text-xs text-red-600"></span>
                        </div>
                    </div>
                    <div>
                        <label asp-for="Notes" class="block text-sm font-medium text-gray-700 mb-1"></label>
                        <textarea asp-for="Notes" rows="3" class="w-full px-3 py-2 border border-gray-300 rounded-md text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500"></textarea>
                        <span asp-validation-for="Notes" class="text-xs text-red-600"></span>
                    </div>
                </div>
            </div>
        </div>
        <div class="space-y-6">
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-5">
                <h2 class="text-lg font-semibold text-gray-900 mb-4">Actions</h2>
                <button type="submit" class="w-full px-4 py-2.5 bg-blue-600 text-white text-sm font-medium rounded-md hover:bg-blue-700 transition-colors flex items-center justify-center gap-2"><i class="fas fa-save"></i> Save Changes</button>
                <a href="/admin/citations" class="w-full block text-center mt-3 px-4 py-2.5 text-sm text-gray-600 bg-gray-100 rounded-md hover:bg-gray-200 transition-colors"><i class="fas fa-arrow-left me-1"></i> Back to List</a>
                <hr class="border-gray-200 my-3" />
                <form method="post" action="/admin/citations/@Model.CitationId/Delete" onsubmit="return confirm('Are you sure you want to delete this citation?');">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="w-full px-4 py-2 text-sm text-red-600 border border-red-200 rounded-md hover:bg-red-50 transition-colors flex items-center justify-center gap-2"><i class="fas fa-trash-alt"></i> Delete</button>
                </form>
            </div>
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-5">
                <h2 class="text-sm font-semibold text-gray-700 mb-2">Info</h2>
                <dl class="space-y-2 text-xs">
                    <div class="flex justify-between"><dt class="text-gray-500">ID</dt><dd class="text-gray-900 font-mono">@Model.CitationId</dd></div>
                </dl>
            </div>
        </div>
    </div>
</form>

<!-- Create Source Modal -->
<div id="create-source-modal" class="fixed inset-0 z-50 hidden overflow-y-auto" aria-labelledby="modal-title" role="dialog" aria-modal="true">
    <div class="flex items-center justify-center min-h-screen p-4">
        <div class="fixed inset-0 bg-gray-500 bg-opacity-50 transition-opacity" onclick="closeSourceModal()"></div>
        <div class="relative bg-white rounded-lg shadow-xl max-w-md w-full p-6">
            <div id="modal-content">
                <div class="text-center py-4">
                    <i class="fas fa-spinner fa-spin text-gray-400 text-2xl"></i>
                    <p class="text-sm text-gray-500 mt-2">Loading...</p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.21.0/jquery.validate.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validation-unobtrusive/4.1.0/jquery.validate.unobtrusive.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var btn = document.getElementById('btn-create-source');
            if (btn) {
                btn.addEventListener('click', function () {
                    openSourceModal();
                });
            }
        });

        function openSourceModal() {
            var modal = document.getElementById('create-source-modal');
            modal.classList.remove('hidden');
            document.body.classList.add('overflow-hidden');

            fetch('/admin/citations/CreateSourceInline')
                .then(function (response) { return response.text(); })
                .then(function (html) {
                    document.getElementById('modal-content').innerHTML = html;
                })
                .catch(function () {
                    document.getElementById('modal-content').innerHTML = '<p class="text-red-500 text-sm">Failed to load form.</p>';
                });
        }

        function closeSourceModal() {
            var modal = document.getElementById('create-source-modal');
            modal.classList.add('hidden');
            document.body.classList.remove('overflow-hidden');
        }

        function submitInlineSource() {
            var form = document.getElementById('inline-source-form');
            if (!form) return;

            var formData = new FormData(form);

            fetch('/admin/citations/CreateSourceInline', {
                method: 'POST',
                body: formData,
                headers: { 'RequestVerificationToken': document.querySelector('[name=__RequestVerificationToken]').value }
            })
            .then(function (response) { return response.json(); })
            .then(function (data) {
                if (data.success) {
                    var select = document.getElementById('SourceId');
                    var option = document.createElement('option');
                    option.value = data.sourceId;
                    option.text = data.title;
                    option.selected = true;
                    select.appendChild(option);
                    closeSourceModal();
                } else {
                    alert(data.message || 'Failed to create source.');
                }
            })
            .catch(function () {
                alert('An error occurred while creating the source.');
            });
        }
    </script>
}
