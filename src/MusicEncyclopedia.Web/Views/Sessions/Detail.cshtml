@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer
@model MusicEncyclopedia.Web.ViewModels.Public.SessionDetailViewModel

@{
    var culture = Context.Items["culture"] as string ?? "fa";
    var dir = Context.Items["dir"] as string ?? "ltr";
    var session = Model.Session;
}

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8" dir="@dir">
    <nav aria-label="@Localizer["Breadcrumb"]" class="mb-6">
        <ol class="flex items-center gap-2 text-sm text-gray-500">
            <li><a href="/@culture" class="hover:text-blue-600">@Localizer["Home"]</a></li>
            <li><span class="mx-1">/</span></li>
            <li><a href="/@culture/sessions" class="hover:text-blue-600">@Localizer["Sessions"]</a></li>
            <li><span class="mx-1">/</span></li>
            <li class="text-gray-900 font-medium truncate max-w-[200px]" aria-current="page">@session.Slug</li>
        </ol>
    </nav>

    <div class="flex flex-col lg:flex-row gap-8">
        <div class="flex-1 min-w-0 space-y-8">
            <!-- Header -->
            <section>
                <h1 class="text-3xl font-bold text-gray-900">@Localizer["Recording Session"]: @session.Slug</h1>
                <div class="flex flex-wrap gap-3 mt-3">
                    @if (!string.IsNullOrWhiteSpace(session.SessionTypeName))
                    {
                        <span class="inline-block bg-blue-100 text-blue-800 text-sm font-medium px-3 py-1 rounded-full">@session.SessionTypeName</span>
                    }
                    @if (session.StartDate.HasValue)
                    {
                        <span class="text-sm text-gray-600">
                            <i class="fas fa-calendar text-gray-400 me-1"></i>
                            @session.StartDate.Value.ToString("yyyy-MM-dd")
                            @if (session.EndDate.HasValue && session.EndDate != session.StartDate)
                            {
                                <span> – @session.EndDate.Value.ToString("yyyy-MM-dd")</span>
                            }
                        </span>
                    }
                </div>
            </section>

            <!-- Location -->
            @if (session.LocationId.HasValue)
            {
                <section class="bg-gray-50 rounded-lg p-5 border border-gray-200">
                    <h2 class="text-lg font-semibold text-gray-900 mb-2">@Localizer["Location"]</h2>
                    <a href="/@culture/locations/@session.LocationSlug" class="text-blue-600 hover:text-blue-800">
                        <i class="fas fa-map-marker-alt me-1"></i> @session.LocationName
                    </a>
                </section>
            }

            <!-- Notes -->
            @if (!string.IsNullOrWhiteSpace(session.Notes))
            {
                <section class="bg-white rounded-lg p-5 border border-gray-200">
                    <h2 class="text-lg font-semibold text-gray-900 mb-2">@Localizer["Notes"]</h2>
                    <div class="text-gray-700 whitespace-pre-line">@session.Notes</div>
                </section>
            }

            <!-- Albums Recorded -->
            @if (session.Albums.Count > 0)
            {
                <section>
                    <h2 class="text-lg font-semibold text-gray-900 mb-3">@Localizer["Albums Recorded"]</h2>
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                        @foreach (var album in session.Albums)
                        {
                            <a href="/@culture/albums/@album.Slug" class="flex items-center gap-3 bg-white rounded-lg border border-gray-200 p-3 hover:shadow-sm transition-shadow">
                                <div class="w-12 h-12 bg-gray-100 rounded flex-shrink-0 flex items-center justify-center text-gray-400">
                                    <i class="fas fa-compact-disc"></i>
                                </div>
                                <div>
                                    <p class="font-medium text-gray-900">@album.Title</p>
                                </div>
                            </a>
                        }
                    </div>
                </section>
            }

            <!-- Tracks Recorded -->
            @if (session.Tracks.Count > 0)
            {
                <section>
                    <h2 class="text-lg font-semibold text-gray-900 mb-3">@Localizer["Tracks Recorded"]</h2>
                    <div class="bg-white rounded-lg border border-gray-200 divide-y">
                        @foreach (var track in session.Tracks)
                        {
                            <a href="/@culture/tracks/@track.Slug" class="flex items-center justify-between p-3 hover:bg-gray-50 transition-colors">
                                <span class="text-gray-900 font-medium">@track.Title</span>
                                @if (track.DurationSeconds.HasValue)
                                {
                                    <span class="text-sm text-gray-500">@TimeSpan.FromSeconds(track.DurationSeconds.Value).ToString(@"m\:ss")</span>
                                }
                            </a>
                        }
                    </div>
                </section>
            }

            <!-- Media -->
            @if (session.Media.Count > 0)
            {
                <section>
                    <h2 class="text-lg font-semibold text-gray-900 mb-3">@Localizer["Media"]</h2>
                    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
                        @foreach (var media in session.Media)
                        {
                            <div class="bg-gray-100 rounded-lg aspect-square overflow-hidden">
                                @if (!string.IsNullOrWhiteSpace(media.Url))
                                {
                                    <img src="@media.Url" alt="@media.FileName" class="w-full h-full object-cover" loading="lazy" />
                                }
                            </div>
                        }
                    </div>
                </section>
            }

            <!-- Citations / References -->
            @if (session.Citations.Count > 0)
            {
                <section>
                    <h2 class="text-lg font-semibold text-gray-900 mb-3">@Localizer["References"]</h2>
                    <ul class="space-y-2">
                        @foreach (var citation in session.Citations)
                        {
                            <li class="bg-white rounded-lg border border-gray-200 p-3 text-sm text-gray-700">
                                @if (!string.IsNullOrWhiteSpace(citation.Quote))
                                {
                                    <blockquote class="italic border-s-4 border-gray-300 ps-3 mb-1">@citation.Quote</blockquote>
                                }
                                @if (!string.IsNullOrWhiteSpace(citation.SourceName))
                                {
                                    <p class="text-xs text-gray-500">— @citation.SourceName</p>
                                }
                            </li>
                        }
                    </ul>
                </section>
            }
        </div>

        <!-- Sidebar -->
        <aside class="w-full lg:w-72 space-y-4">
            <div class="bg-white rounded-lg border border-gray-200 p-4">
                <h3 class="text-sm font-semibold text-gray-700 mb-2">@Localizer["Session Info"]</h3>
                <dl class="space-y-1 text-sm">
                    <div class="flex justify-between">
                        <dt class="text-gray-500">@Localizer["Type"]</dt>
                        <dd class="text-gray-900">@(session.SessionTypeName ?? "—")</dd>
                    </div>
                    <div class="flex justify-between">
                        <dt class="text-gray-500">@Localizer["Start Date"]</dt>
                        <dd class="text-gray-900">@(session.StartDate?.ToString("yyyy-MM-dd") ?? "—")</dd>
                    </div>
                    <div class="flex justify-between">
                        <dt class="text-gray-500">@Localizer["End Date"]</dt>
                        <dd class="text-gray-900">@(session.EndDate?.ToString("yyyy-MM-dd") ?? "—")</dd>
                    </div>
                </dl>
            </div>
        </aside>
    </div>
</div>

<!-- ============================================================
     JSON-LD Structured Data: Event (RecordingSession)
     ============================================================ -->
@section StructuredData {
<script type="application/ld+json">
{
    "@@context": "https://schema.org",
    "@@type": "Event",
    "name": "@Model.Session.Slug",
    @if (Model.Session.StartDate.HasValue)
    {
        <text>"startDate": "@Model.Session.StartDate.Value.ToString("yyyy-MM-dd")",</text>
    }
    @if (Model.Session.EndDate.HasValue)
    {
        <text>"endDate": "@Model.Session.EndDate.Value.ToString("yyyy-MM-dd")",</text>
    }
    @if (!string.IsNullOrWhiteSpace(Model.Session.Notes))
    {
        <text>"description": "@Model.Session.Notes",</text>
    }
    @if (Model.Session.LocationId.HasValue && !string.IsNullOrWhiteSpace(Model.Session.LocationName))
    {
        <text>"location": {
            "@@type": "Place",
            "name": "@Model.Session.LocationName"
        },</text>
    }
    "url": "@($"{Context.Request.Scheme}://{Context.Request.Host}/{culture}/sessions/{Model.Session.Slug}")"
}
</script>
}
