@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer
@model MusicEncyclopedia.Web.ViewModels.Public.SessionListViewModel

@{
    var culture = Context.Items["culture"] as string ?? "fa";
    var dir = Context.Items["dir"] as string ?? "ltr";
    var isRtl = dir == "rtl";
    var items = Model.Items;
}

<link rel="canonical" href="@($"{Context.Request.Scheme}://{Context.Request.Host}{Context.Request.PathBase}{Context.Request.Path}")" />

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8" dir="@dir">
    <div class="mb-8">
        <h1 class="text-3xl md:text-4xl font-bold text-gray-900">@Localizer["Recording Sessions"]</h1>
        <p class="mt-2 text-gray-600">@Localizer["Browse recording sessions"]</p>
    </div>

    @if (items.Items.Count == 0)
    {
        <div class="text-center py-16">
            <i class="fas fa-circle text-5xl text-gray-300 mb-4 block"></i>
            <h3 class="text-lg font-medium text-gray-900">@Localizer["No sessions found"]</h3>
        </div>
    }
    else
    {
        <p class="text-sm text-gray-500 mb-4">
            @Localizer["Showing {0}–{1} of {2} sessions",
                ((items.Page - 1) * items.PageSize) + 1,
                Math.Min(items.Page * items.PageSize, items.TotalItems),
                items.TotalItems]
        </p>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            @foreach (var session in items.Items)
            {
                <article class="bg-white rounded-lg border border-gray-200 overflow-hidden shadow-sm hover:shadow-md transition-shadow">
                    <a href="/@culture/sessions/@session.Slug" class="block p-5">
                        <div class="flex items-center gap-2 mb-2">
                            @if (!string.IsNullOrWhiteSpace(session.SessionTypeName))
                            {
                                <span class="inline-block bg-blue-100 text-blue-800 text-xs font-medium px-2 py-0.5 rounded-full">
                                    @session.SessionTypeName
                                </span>
                            }
                            @if (session.StartDate.HasValue)
                            {
                                <span class="text-xs text-gray-500">@session.StartDate.Value.ToString("yyyy-MM-dd")</span>
                            }
                            @if (session.EndDate.HasValue && session.EndDate != session.StartDate)
                            {
                                <span class="text-xs text-gray-400">– @session.EndDate.Value.ToString("yyyy-MM-dd")</span>
                            }
                        </div>
                        <h3 class="font-semibold text-gray-900">@session.Slug</h3>
                        @if (!string.IsNullOrWhiteSpace(session.LocationName))
                        {
                            <p class="text-sm text-gray-500 mt-1">
                                <i class="fas fa-map-marker-alt text-gray-400 me-1"></i> @session.LocationName
                            </p>
                        }
                        @if (!string.IsNullOrWhiteSpace(session.NotesPreview))
                        {
                            <p class="text-sm text-gray-600 mt-2 line-clamp-2">@session.NotesPreview</p>
                        }
                    </a>
                </article>
            }
        </div>

        if (items.TotalPages > 1)
        {
            <nav class="mt-10 flex items-center justify-center gap-1" aria-label="@Localizer["Pagination"]">
                @if (items.Page > 1)
                {
                    <a href="/@culture/sessions?page=@(items.Page - 1)"
                       class="px-3 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50"
                       rel="prev">@Localizer["Previous"]</a>
                }
                <span class="px-3 py-2 text-sm font-medium text-white bg-blue-600 border border-blue-600 rounded-md">@items.Page</span>
                @if (items.Page < items.TotalPages)
                {
                    <a href="/@culture/sessions?page=@(items.Page + 1)"
                       class="px-3 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50"
                       rel="next">@Localizer["Next"]</a>
                }
            </nav>
        }
    }
</div>
