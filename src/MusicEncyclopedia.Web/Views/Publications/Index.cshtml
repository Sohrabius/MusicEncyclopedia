@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer
@model MusicEncyclopedia.Web.ViewModels.Public.PublicationListViewModel

@{
    var culture = Context.Items["culture"] as string ?? "fa";
    var dir = Context.Items["dir"] as string ?? "ltr";
    var items = Model.Items;
}

<link rel="canonical" href="@($"{Context.Request.Scheme}://{Context.Request.Host}{Context.Request.PathBase}{Context.Request.Path}")" />

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8" dir="@dir">
    <div class="mb-8">
        <h1 class="text-3xl md:text-4xl font-bold text-ink">@Localizer["Publications"]</h1>
        <p class="mt-2 text-ink-secondary">@Localizer["Browse publications and authored works"]</p>
    </div>

    @if (items.Items.Count == 0)
    {
        <div class="text-center py-16">
            <i aria-hidden="true" class="fas fa-book text-5xl text-ink-disabled mb-4 block"></i>
            <h3 class="text-lg font-medium text-ink">@Localizer["No publications found"]</h3>
        </div>
    }
    else
    {
        <p class="text-sm text-ink-muted mb-4">
            @Localizer["Showing {0}–{1} of {2} publications",
                ((items.Page - 1) * items.PageSize) + 1,
                Math.Min(items.Page * items.PageSize, items.TotalItems),
                items.TotalItems]
        </p>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            @foreach (var publication in items.Items)
            {
                <article class="bg-surface rounded-lg border border-rule overflow-hidden shadow-sm hover:shadow-md transition-shadow">
                    <a href="/@culture/publications/@publication.Slug" class="block p-5">
                        <h3 class="font-semibold text-ink">@publication.Title</h3>
                        <div class="flex flex-wrap gap-2 mt-2">
                            @if (!string.IsNullOrWhiteSpace(publication.PublicationTypeName))
                            {
                                <span class="inline-block bg-info-soft text-info-hover text-xs font-medium px-2 py-0.5 rounded-full">@publication.PublicationTypeName</span>
                            }
                        </div>
                        @if (!string.IsNullOrWhiteSpace(publication.PublisherName))
                        {
                            <p class="text-sm text-ink-muted mt-1"><i aria-hidden="true" class="fas fa-building text-ink-muted me-1"></i>@publication.PublisherName</p>
                        }
                        @if (publication.PublicationDate.HasValue)
                        {
                            <p class="text-xs text-ink-muted mt-1">@publication.PublicationDate.Value.ToString("yyyy-MM-dd")</p>
                        }
                    </a>
                </article>
            }
        </div>
        if (items.TotalPages > 1)
        {
            <nav class="mt-10 flex items-center justify-center gap-1">
                @if (items.Page > 1)
                {
                    <a href="/@culture/publications?page=@(items.Page - 1)" class="px-3 py-2 text-sm font-medium text-ink-secondary bg-surface border border-rule-strong rounded-md hover:bg-paper" rel="prev">@Localizer["Previous"]</a>
                }
                <span class="px-3 py-2 text-sm font-medium text-on-accent bg-accent border border-accent rounded-md">@items.Page</span>
                @if (items.Page < items.TotalPages)
                {
                    <a href="/@culture/publications?page=@(items.Page + 1)" class="px-3 py-2 text-sm font-medium text-ink-secondary bg-surface border border-rule-strong rounded-md hover:bg-paper" rel="next">@Localizer["Next"]</a>
                }
            </nav>
        }
    }
</div>
