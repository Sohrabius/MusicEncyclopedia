@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer
@model MusicEncyclopedia.Web.ViewModels.Public.AwardListViewModel

@{
    var culture = Context.Items["culture"] as string ?? "fa";
    var dir = Context.Items["dir"] as string ?? "ltr";
    var items = Model.Items;
}

<link rel="canonical" href="@($"{Context.Request.Scheme}://{Context.Request.Host}{Context.Request.PathBase}{Context.Request.Path}")" />

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8" dir="@dir">
    <div class="mb-8">
        <h1 class="text-3xl md:text-4xl font-bold text-ink">@Localizer["Awards"]</h1>
        <p class="mt-2 text-ink-secondary">@Localizer["Browse awards and honors"]</p>
    </div>

    @if (items.Items.Count == 0)
    {
        <div class="text-center py-16">
            <i aria-hidden="true" class="fas fa-trophy text-5xl text-ink-disabled mb-4 block"></i>
            <h3 class="text-lg font-medium text-ink">@Localizer["No awards found"]</h3>
        </div>
    }
    else
    {
        <p class="text-sm text-ink-muted mb-4">
            @Localizer["Showing {0}–{1} of {2} awards",
                ((items.Page - 1) * items.PageSize) + 1,
                Math.Min(items.Page * items.PageSize, items.TotalItems),
                items.TotalItems]
        </p>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            @foreach (var award in items.Items)
            {
                <article class="bg-surface rounded-lg border border-rule overflow-hidden shadow-sm hover:shadow-md transition-shadow">
                    <a href="/@culture/awards/@award.Slug" class="block p-5">
                        <h3 class="font-semibold text-ink text-lg">@award.Name</h3>
                        <div class="flex flex-wrap gap-2 mt-2">
                            @if (!string.IsNullOrWhiteSpace(award.Organization))
                            {
                                <span class="text-sm text-ink-secondary"><i aria-hidden="true" class="fas fa-building text-ink-muted me-1"></i>@award.Organization</span>
                            }
                            @if (!string.IsNullOrWhiteSpace(award.CountryName))
                            {
                                <span class="text-xs text-ink-muted"><i aria-hidden="true" class="fas fa-globe text-ink-muted me-1"></i>@award.CountryName</span>
                            }
                        </div>
                        @if (!string.IsNullOrWhiteSpace(award.DescriptionPreview))
                        {
                            <p class="text-sm text-ink-secondary mt-2 line-clamp-2">@award.DescriptionPreview</p>
                        }
                    </a>
                </article>
            }
        </div>
        if (items.TotalPages > 1)
        {
            <nav class="mt-10 flex items-center justify-center gap-1">
                @if (items.Page > 1)
                {
                    <a href="/@culture/awards?page=@(items.Page - 1)" class="px-3 py-2 text-sm font-medium text-ink-secondary bg-surface border border-rule-strong rounded-md hover:bg-paper" rel="prev">@Localizer["Previous"]</a>
                }
                <span class="px-3 py-2 text-sm font-medium text-on-accent bg-accent border border-accent rounded-md">@items.Page</span>
                @if (items.Page < items.TotalPages)
                {
                    <a href="/@culture/awards?page=@(items.Page + 1)" class="px-3 py-2 text-sm font-medium text-ink-secondary bg-surface border border-rule-strong rounded-md hover:bg-paper" rel="next">@Localizer["Next"]</a>
                }
            </nav>
        }
    }
</div>
