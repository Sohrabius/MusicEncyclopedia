@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer
@model MusicEncyclopedia.Web.ViewModels.Public.AwardDetailViewModel

@{
    var culture = Context.Items["culture"] as string ?? "fa";
    var dir = Context.Items["dir"] as string ?? "ltr";
    var award = Model.Award;
}

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8" dir="@dir">
    <nav aria-label="@Localizer["Breadcrumb"]" class="mb-6">
        <ol class="flex items-center gap-2 text-sm text-ink-muted">
            <li><a href="/@culture" class="hover:text-accent">@Localizer["Home"]</a></li>
            <li><a href="/@culture/awards" class="hover:text-accent ms-1">@Localizer["Awards"]</a></li>
            <li><span class="mx-1">/</span></li>
            <li class="text-ink font-medium truncate max-w-[200px]" aria-current="page">@award.Name</li>
        </ol>
    </nav>

    <div class="flex flex-col lg:flex-row gap-8">
        <div class="flex-1 min-w-0 space-y-8">
            <section>
                <h1 class="text-3xl font-bold text-ink">@award.Name</h1>
                <div class="flex flex-wrap gap-3 mt-3">
                    @if (!string.IsNullOrWhiteSpace(award.Organization))
                    {
                        <span class="text-sm text-ink-secondary"><i aria-hidden="true" class="fas fa-building text-ink-muted me-1"></i> @award.Organization</span>
                    }
                    @if (!string.IsNullOrWhiteSpace(award.CountryName))
                    {
                        <span class="text-sm text-ink-secondary"><i aria-hidden="true" class="fas fa-globe text-ink-muted me-1"></i> @award.CountryName</span>
                    }
                </div>
            </section>

            @if (!string.IsNullOrWhiteSpace(award.Description))
            {
                <section class="bg-surface rounded-lg p-5 border border-rule">
                    <h2 class="text-lg font-semibold text-ink mb-2">@Localizer["Description"]</h2>
                    <div class="text-ink-secondary whitespace-pre-line">@award.Description</div>
                </section>
            }

            @if (award.Assignments.Count > 0)
            {
                <section>
                    <h2 class="text-lg font-semibold text-ink mb-3">@Localizer["Winners & Nominees"]</h2>
                    <div class="bg-surface border border-rule rounded-lg overflow-hidden">
                        <table class="w-full text-sm">
                            <thead class="bg-paper border-b border-rule">
                                <tr>
                                    <th class="text-start px-4 py-2 font-medium text-ink-secondary">@Localizer["Year"]</th>
                                    <th class="text-start px-4 py-2 font-medium text-ink-secondary">@Localizer["Category"]</th>
                                    <th class="text-start px-4 py-2 font-medium text-ink-secondary">@Localizer["Entity"]</th>
                                    <th class="text-start px-4 py-2 font-medium text-ink-secondary">@Localizer["Result"]</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-rule-light">
                                @foreach (var assignment in award.Assignments)
                                {
                                    <tr class="hover:bg-paper">
                                        <td class="px-4 py-2 text-ink">@assignment.Year?.ToString()</td>
                                        <td class="px-4 py-2 text-ink-secondary">@(assignment.Category ?? "—")</td>
                                        <td class="px-4 py-2 text-ink">@assignment.EntityName</td>
                                        <td class="px-4 py-2">
                                            <span class="inline-block px-2 py-0.5 text-xs rounded-full @(assignment.ResultTypeCode == "WINNER" ? "bg-success-soft text-success-hover" : "bg-paper-muted text-ink-secondary")">
                                                @(assignment.ResultTypeName ?? "Nominated")
                                            </span>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </section>
            }
        </div>

        <aside class="w-full lg:w-72 space-y-4">
            <div class="bg-surface rounded-lg border border-rule p-4">
                <h3 class="text-sm font-semibold text-ink-secondary mb-2">@Localizer["Award Info"]</h3>
                <dl class="space-y-1 text-sm">
                    <div class="flex justify-between"><dt class="text-ink-muted">@Localizer["Organization"]</dt><dd class="text-ink">@(award.Organization ?? "—")</dd></div>
                    <div class="flex justify-between"><dt class="text-ink-muted">@Localizer["Country"]</dt><dd class="text-ink">@(award.CountryName ?? "—")</dd></div>
                    <div class="flex justify-between"><dt class="text-ink-muted">@Localizer["Assignments"]</dt><dd class="text-ink">@award.Assignments.Count</dd></div>
                </dl>
            </div>
        </aside>
    </div>
</div>

<!-- ============================================================
     JSON-LD Structured Data: Award
     ============================================================ -->
@section StructuredData {
<script type="application/ld+json">
{
    "@@context": "https://schema.org",
    "@@type": "Thing",
    "name": "@Model.Award.Name",
    @if (!string.IsNullOrWhiteSpace(Model.Award.Description))
    {
        <text>"description": "@Model.Award.Description",</text>
    }
    @if (!string.IsNullOrWhiteSpace(Model.Award.Organization))
    {
        <text>"publisher": {
            "@@type": "Organization",
            "name": "@Model.Award.Organization"
        },</text>
    }
    "url": "@($"{Context.Request.Scheme}://{Context.Request.Host}/{culture}/awards/{Model.Award.Slug}")"
}
</script>
}
