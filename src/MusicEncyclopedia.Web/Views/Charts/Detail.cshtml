@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer
@model MusicEncyclopedia.Web.ViewModels.Public.ChartDetailViewModel

@{
    var culture = Context.Items["culture"] as string ?? "fa";
    var dir = Context.Items["dir"] as string ?? "ltr";
    var chart = Model.Chart;
}

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8" dir="@dir">
    <nav aria-label="@Localizer["Breadcrumb"]" class="mb-6">
        <ol class="flex items-center gap-2 text-sm text-ink-muted">
            <li><a href="/@culture" class="hover:text-accent">@Localizer["Home"]</a></li>
            <li><a href="/@culture/charts" class="hover:text-accent ms-1">@Localizer["Charts"]</a></li>
            <li><span class="mx-1">/</span></li>
            <li class="text-ink font-medium truncate max-w-[200px]" aria-current="page">@chart.Name</li>
        </ol>
    </nav>

    <div class="flex flex-col lg:flex-row gap-8">
        <div class="flex-1 min-w-0 space-y-8">
            <section>
                <h1 class="text-3xl font-bold text-ink">@chart.Name</h1>
                <div class="flex flex-wrap gap-3 mt-3">
                    @if (!string.IsNullOrWhiteSpace(chart.Publisher))
                    {
                        <span class="text-sm text-ink-secondary"><i aria-hidden="true" class="fas fa-building text-ink-muted me-1"></i> @chart.Publisher</span>
                    }
                    @if (!string.IsNullOrWhiteSpace(chart.CountryName))
                    {
                        <span class="text-sm text-ink-secondary"><i aria-hidden="true" class="fas fa-globe text-ink-muted me-1"></i> @chart.CountryName</span>
                    }
                    @if (!string.IsNullOrWhiteSpace(chart.Frequency))
                    {
                        <span class="inline-block bg-warning-soft text-warning-hover text-sm font-medium px-3 py-1 rounded-full">@chart.Frequency</span>
                    }
                </div>
            </section>

            @if (chart.Entries.Count > 0)
            {
                <section>
                    <h2 class="text-lg font-semibold text-ink mb-3">@Localizer["Chart Entries"]</h2>
                    <div class="bg-surface border border-rule rounded-lg overflow-hidden">
                        <table class="w-full text-sm">
                            <thead class="bg-paper border-b border-rule">
                                <tr>
                                    <th class="text-start px-4 py-2 font-medium text-ink-secondary">@Localizer["Date"]</th>
                                    <th class="text-start px-4 py-2 font-medium text-ink-secondary">@Localizer["Entity"]</th>
                                    <th class="text-center px-4 py-2 font-medium text-ink-secondary">@Localizer["Position"]</th>
                                    <th class="text-center px-4 py-2 font-medium text-ink-secondary">@Localizer["Prev"]</th>
                                    <th class="text-center px-4 py-2 font-medium text-ink-secondary">@Localizer["Weeks"]</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-rule-light">
                                @foreach (var entry in chart.Entries)
                                {
                                    <tr class="hover:bg-paper">
                                        <td class="px-4 py-2 text-ink-muted whitespace-nowrap">@entry.Date.ToString("yyyy-MM-dd")</td>
                                        <td class="px-4 py-2">
                                            @if (!string.IsNullOrWhiteSpace(entry.EntitySlug))
                                            {
                                                <a href="/@culture/@(entry.EntityTypeId == 1 ? "albums" : entry.EntityTypeId == 2 ? "tracks" : "people")/@entry.EntitySlug"
                                                   class="text-accent hover:text-accent-hover font-medium">@entry.EntityTitle</a>
                                            }
                                            else
                                            {
                                                <span class="text-ink">@entry.EntityTitle ?? "Unknown"</span>
                                            }
                                        </td>
                                        <td class="px-4 py-2 text-center font-bold text-ink">#@entry.Position</td>
                                        <td class="px-4 py-2 text-center text-ink-muted">
                                            @if (entry.PreviousPosition.HasValue)
                                            {
                                                var diff = entry.PreviousPosition.Value - entry.Position;
                                                if (diff > 0) { <span class="text-success">▲@diff</span> }
                                                else if (diff < 0) { <span class="text-error">▼@(-diff)</span> }
                                                else { <span class="text-ink-muted">—</span> }
                                            }
                                            else { <span class="text-ink-muted">—</span> }
                                        </td>
                                        <td class="px-4 py-2 text-center text-ink-muted">@(entry.WeeksOnChart?.ToString() ?? "—")</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </section>
            }
        </div>

        <aside class="w-full lg:w-72 space-y-4">
            <div class="bg-surface rounded-lg border border-rule p-4">
                <h3 class="text-sm font-semibold text-ink-secondary mb-2">@Localizer["Chart Info"]</h3>
                <dl class="space-y-1 text-sm">
                    <div class="flex justify-between"><dt class="text-ink-muted">@Localizer["Publisher"]</dt><dd class="text-ink">@(chart.Publisher ?? "—")</dd></div>
                    <div class="flex justify-between"><dt class="text-ink-muted">@Localizer["Country"]</dt><dd class="text-ink">@(chart.CountryName ?? "—")</dd></div>
                    <div class="flex justify-between"><dt class="text-ink-muted">@Localizer["Frequency"]</dt><dd class="text-ink">@(chart.Frequency ?? "—")</dd></div>
                    <div class="flex justify-between"><dt class="text-ink-muted">@Localizer["Entries"]</dt><dd class="text-ink">@chart.Entries.Count</dd></div>
                </dl>
            </div>
        </aside>
    </div>
</div>

<!-- ============================================================
     JSON-LD Structured Data: CreativeWork (Chart)
     ============================================================ -->
@section StructuredData {
<script type="application/ld+json">
{
    "@@context": "https://schema.org",
    "@@type": "CreativeWork",
    "name": "@Model.Chart.Name",
    @if (!string.IsNullOrWhiteSpace(Model.Chart.Publisher))
    {
        <text>"publisher": {
            "@@type": "Organization",
            "name": "@Model.Chart.Publisher"
        },</text>
    }
    @if (!string.IsNullOrWhiteSpace(Model.Chart.Frequency))
    {
        <text>"description": "@Model.Chart.Frequency",</text>
    }
    @if (!string.IsNullOrWhiteSpace(Model.Chart.CountryName))
    {
        <text>"contentLocation": "@Model.Chart.CountryName",</text>
    }
    "url": "@($"{Context.Request.Scheme}://{Context.Request.Host}/{culture}/charts/{Model.Chart.Slug}")"
}
</script>
}
